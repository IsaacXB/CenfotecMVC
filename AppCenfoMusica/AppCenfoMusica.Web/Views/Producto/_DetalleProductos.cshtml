@model List<AppCenfoMusica.DTO.ProductoDTO>

@foreach (var item in Model)
{
    <tr>
        <td>@item.IdEntidad</td>
        <td>@item.Nombre</td>
        <td>@item.PrecioUnitario</td>
        <td>@item.CantidadBodega</td>
        <td>@item.TipoProducto</td>
        <td>
            <div class="btn-group">
@*                @if (ViewBag.UserType == "Vendedor")
                {
                    @Html.ActionLink("Editar", "ActualizarProducto", new { id = item.IdEntidad }, new { @class = "btn btn-outline-primary" })
                }*@
                @Html.ActionLink("Detalles", "BuscarProductoPorID", new { id = item.IdEntidad }, new { @class = "btn btn-outline-primary" })
                @if (ViewBag.UserType == "Vendedor")
                {
                    @Html.ActionLink("Eliminar", "EliminarProducto", new { id = item.IdEntidad }, new { @class = "btn btn-outline-danger" })

                }
            </div>
        </td>
    </tr>
}
